import{_ as e,W as a,X as n,Y as s}from"./framework-9a5142fa.js";const i={},r=s(`<ul><li><a href="#1-branch">1. branch</a><ul><li><a href="#11-add-list-remove">1.1. add list remove</a></li><li><a href="#12-switch-branch">1.2. switch branch</a></li><li><a href="#13-rename-a-branch">1.3. rename a branch</a></li><li><a href="#14-merge">1.4. merge</a></li></ul></li></ul><h1 id="_1-branch" tabindex="-1"><a class="header-anchor" href="#_1-branch" aria-hidden="true">#</a> 1. branch</h1><h2 id="_1-1-add-list-remove" tabindex="-1"><a class="header-anchor" href="#_1-1-add-list-remove" aria-hidden="true">#</a> 1.1. add list remove</h2><p>add a branch</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># git branch &lt;branch&gt;</span>
$ <span class="token function">git</span> branch orange
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>创建分支的效果相当于虚拟机的快照。 PS：注意是对当前的 <strong>commit到本地仓库中的</strong> 的文件进行镜像。</p><blockquote><p>show branch</p></blockquote><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>$ <span class="token function">git</span> branch
* main
  orange
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>git branch</code>列出的分支前带 <code>*</code>号的表示当前分支。</p><blockquote><p>remove branch</p></blockquote><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># 同当前分支一样，即merge过的，那么就不会error</span>
$ <span class="token function">git</span> branch <span class="token parameter variable">-d</span> <span class="token operator">&lt;</span>branch<span class="token operator">&gt;</span>

<span class="token comment"># 同当前分支不一样，所以要强制删除</span>
$ <span class="token function">git</span> branch <span class="token parameter variable">-D</span> <span class="token operator">&lt;</span>branch<span class="token operator">&gt;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="_1-2-switch-branch" tabindex="-1"><a class="header-anchor" href="#_1-2-switch-branch" aria-hidden="true">#</a> 1.2. switch branch</h2><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>$ <span class="token function">git</span> checkout orange
Switched to branch <span class="token string">&#39;orange&#39;</span>
M       branch.md
M       config.md
D       renmote.md
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>当你切换到某分支时，工作目录下的文件就是那个分支 <strong>commit到本地仓库中的</strong> 的文件。</p><p>PS: 切换分支要求你工作区和暂存区没有东西。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># add a branch and switch to it. \`-b\` means branch.</span>
$ <span class="token function">git</span> checkout <span class="token parameter variable">-b</span> <span class="token operator">&lt;</span>branch<span class="token operator">&gt;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="_1-3-rename-a-branch" tabindex="-1"><a class="header-anchor" href="#_1-3-rename-a-branch" aria-hidden="true">#</a> 1.3. rename a branch</h2><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>$ <span class="token function">git</span> branch <span class="token parameter variable">-M</span> main
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>-M: 
Shortcut for --move --force.

-m/--move:
Move/rename a branch, together with its config and reflog.

-f/--force
Reset &lt;branchname&gt; to &lt;startpoint&gt;, even if &lt;branchname&gt; exists already. Without -f, git branch refuses to change an existing branch. In combination with -d (or --delete), allow deleting the branch irrespective of its merged status, or whether it even points to a valid commit. In combination with -m (or --move), allow renaming the branch even if the new branch name already exists, the same applies for -c (or --copy).
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>i.e. 想要重命名已存在的分支，重命名是 <code>-m</code>，而修改已存在需要 <code>--force</code>，<code>-M = -m -f</code>。</p><h2 id="_1-4-merge" tabindex="-1"><a class="header-anchor" href="#_1-4-merge" aria-hidden="true">#</a> 1.4. merge</h2><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>$ <span class="token function">git</span> merge <span class="token operator">&lt;</span>other branch<span class="token operator">&gt;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>git的主要工作流程就是，大家提交不同的分支，然后将其融合到主分支上，一起维护一个主分支。</p><p>所以，merge的意思不是当前分支被别的分支融合，而是当前分支吞噬别的分支（别的分支被当前分支融合后，别的分支消失，只剩下当前分支）。</p><p>Rules:</p><ul><li>某分支中有的，当前分支中没有：则添加到当前分支。</li><li>√某分支中没有的，当前分支中有：则<strong>删除</strong>当前分支中的。</li><li>某分支中有的，当前分支中没有：如无不同，则无事。如有不同，则冲突。</li></ul><p>使用 <code>git diff</code>查看冲突之处。会用 <code>-</code>、<code>+</code>和红绿颜色标识。</p><p><img src="https://cdn.jsdelivr.net/gh/sword4869/pic1@main/images/202406231931649.png" alt="Alt text"></p>`,28),c=[r];function d(t,l){return a(),n("div",null,c)}const h=e(i,[["render",d],["__file","branch.html.vue"]]);export{h as default};
