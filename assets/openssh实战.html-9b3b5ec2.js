import{_ as s,W as e,X as n,Y as a}from"./framework-9a5142fa.js";const i={},o=a(`<p>[toc]</p><h2 id="配置密钥认证" tabindex="-1"><a class="header-anchor" href="#配置密钥认证" aria-hidden="true">#</a> 配置密钥认证</h2><p>（1）客户端生成密钥</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>ssh-keygen
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>（2）确保服务端已生成密钥</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># 确保有 sshd_config中指定的 HostKey 存在</span>
<span class="token function">ls</span> /etc/ssh
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>（3）客户端上传密钥到服务器的可信列表中</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>client$ <span class="token function">scp</span> /home/sword/.ssh/id_rsa.pub coco@192.168.112.130:~
server$ <span class="token function">cat</span> /home/coco/id_rsa.pub <span class="token operator">&gt;&gt;</span> /home/coco/.ssh/authorized_keys
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>确保其权限正确</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>server$ <span class="token function">chmod</span> <span class="token number">700</span> .ssh
server$ <span class="token function">chmod</span> <span class="token number">600</span> .ssh/authorized_keys
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>（4）修改服务端的sshd_config</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> <span class="token function">vim</span> /etc/ssh/sshd_config

PubkeyAuthnentication <span class="token function">yes</span>
PasswordAuthentication no
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>（5）重启服务端的sshd</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> <span class="token function">service</span> <span class="token function">ssh</span> reload		<span class="token comment"># ubuntu 是 ssh，centos是sshd</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h2 id="登陆问题" tabindex="-1"><a class="header-anchor" href="#登陆问题" aria-hidden="true">#</a> 登陆问题</h2><h3 id="warning-remote-host-identification-has-changed" tabindex="-1"><a class="header-anchor" href="#warning-remote-host-identification-has-changed" aria-hidden="true">#</a> WARNING: REMOTE HOST IDENTIFICATION HAS CHANGED!</h3><p>这里要登陆的 server 变化了，因为你 client 本地存有以前记住的 server 的 hostname，所以发现不匹配后的问题。</p><p>解决办法就是删除<strong>client</strong>中的 <code>~/.ssh/known_hosts</code> 的对应的服务器ip的记录。</p><h3 id="warning-unprotected-private-key-file" tabindex="-1"><a class="header-anchor" href="#warning-unprotected-private-key-file" aria-hidden="true">#</a> WARNING: UNPROTECTED PRIVATE KEY FILE!</h3><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>$ /usr/sbin/sshd <span class="token parameter variable">-h</span> /home/sword/.ssh/id_rsa
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
@         WARNING: UNPROTECTED PRIVATE KEY FILE<span class="token operator">!</span>          @
@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@
Permissions 0644 <span class="token keyword">for</span> <span class="token string">&#39;/home/sword/.ssh/known_hosts&#39;</span> are too open.
It is required that your private key files are NOT accessible by others.
This private key will be ignored.
Unable to load <span class="token function">host</span> key <span class="token string">&quot;/home/sword/.ssh/known_hosts&quot;</span><span class="token builtin class-name">:</span> bad permissions
Unable to load <span class="token function">host</span> key: /home/sword/.ssh/known_hosts
sshd: no hostkeys available -- exiting.
【这个情况是说/home/sword/.ssh/id_rsa的文件权限太开放，容易被骇客入侵，我们修改权限就行】

<span class="token comment"># 调整私钥的文件权限</span>
$ <span class="token function">chmod</span> 0600 /home/sword/.ssh/id_rsa
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h3 id="使用-ssh-连接报-bad-owner-or-permissions-on-c-users-administrator-ssh-config-错误问题解决" tabindex="-1"><a class="header-anchor" href="#使用-ssh-连接报-bad-owner-or-permissions-on-c-users-administrator-ssh-config-错误问题解决" aria-hidden="true">#</a> 使用 SSH 连接报 Bad owner or permissions on C:\\Users\\Administrator/.ssh/config 错误问题解决</h3><blockquote><p>问题描述</p></blockquote><p>在 Windows 系统下的 VSCode 安装 Remote - SSH 扩展后，使用扩展配置 SSH 并进行远程连接，可能会发生 Bad owner or permissions on C:\\Users\\Administrator/.ssh/config 错误，造成无法进行 SSH 远程连接的问题。</p><p>原因是由于使用 Remote - SSH 扩展所依赖的 Remote - SSH: Editing Configuration Files 扩展编辑了 C:\\Users\\Administrator.ssh\\config 文件后，此文件的权限发生了改变</p><p>而把此配置文件删除后，使用 PowerShell 即可正常进行远程连接。但 VSCode 的 SSH 连接又依赖此配置文件，所以就产生了冲突，要么只有 PowerShell 能用，要么就都不能用。</p><blockquote><p>解决办法</p></blockquote><ol><li><p>在 GitHub 上下载 openssh-portable 项目，其 Git 命令如下：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">git</span> clone https://github.com/PowerShell/openssh-portable.git
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li><li><p>下载完成后进入 openssh-portable 项目中的 <code>contrib\\win32\\openssh</code> 目录</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token builtin class-name">cd</span> openssh-portable/contrib/win32/openssh
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div></li><li><p>在此目录中以管理员权限打开powershell，执行以下命令：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>./FixUserFilePermissions.ps1 -Confirm:<span class="token variable">$false</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>PS: 执行powershell脚本出错：未对文件进行数字签名：以管理员权限打开powershell</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># Y</span>
set-ExecutionPolicy RemoteSigned

<span class="token comment"># 为RemoteSigned表示成功</span>
get-executionpolicy
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>右击powershell脚本文件，选中解除锁定，并应用 <img src="https://img-blog.csdnimg.cn/20210706173551640.png" alt=""></p></li></ol><h3 id="no-matching-host-key-type-found-their-offer-ssh-rsa" tabindex="-1"><a class="header-anchor" href="#no-matching-host-key-type-found-their-offer-ssh-rsa" aria-hidden="true">#</a> No matching host key type found. Their offer: ssh-rsa</h3><p>openssh觉得ssh-rsa加密方式不安全，从8.8开始的 openssh 的<strong>客户端</strong>默认禁用了 <code>ssh-rsa</code> 算法</p><p>解决：让客户端允许接受rsa算法。</p><ul><li>临时性方案命令行增加参数</li></ul><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">ssh</span> <span class="token parameter variable">-o</span> <span class="token assign-left variable">HostKeyAlgorithms</span><span class="token operator">=</span>+ssh-rsa <span class="token parameter variable">-o</span> <span class="token assign-left variable">PubkeyAcceptedKeyTypes</span><span class="token operator">=</span>+ssh-rsa  user@host
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><ul><li>配置文件持久化: 将需要连接的服务器IP加入到host，可以用 <code>*</code> 对所有主机生效</li></ul><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">sudo</span> <span class="token function">vim</span> ~/.ssh/config


Host *
    HostKeyAlgorithms +ssh-rsa
    PubkeyAcceptedKeyTypes +ssh-rsa
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,34),d=[o];function l(r,c){return e(),n("div",null,d)}const p=s(i,[["render",l],["__file","openssh实战.html.vue"]]);export{p as default};
