import{_ as a,W as s,X as n,Y as e}from"./framework-9a5142fa.js";const i={},t=e(`<p>[toc]</p><ul><li>对于很大的文件，可以只用很小的空间(lfs 指针)来管理它。</li></ul><h2 id="_1-install" tabindex="-1"><a class="header-anchor" href="#_1-install" aria-hidden="true">#</a> 1. install</h2><p>windows下的git已经集成了git lfs.</p><p>linux: <code>sudo apt install git git-lfs</code></p><p>wsl同linux</p><p>然后对该机器的当前用户进行全局初始化，所以新机器、新用户要执行一次，已经执行过的就不用执行了（PS: 取消初始化<code>git lfs uninstall</code>）</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># 会去修改 git config --global 的东西</span>
$ <span class="token function">git</span> lfs <span class="token function">install</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>$ <span class="token function">git</span> lfs version
git-lfs/3.3.0 <span class="token punctuation">(</span>GitHub<span class="token punctuation">;</span> windows amd64<span class="token punctuation">;</span> go <span class="token number">1.19</span>.3<span class="token punctuation">;</span> <span class="token function">git</span> 77deabdf<span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="_2-下载" tabindex="-1"><a class="header-anchor" href="#_2-下载" aria-hidden="true">#</a> 2. 下载</h2><p>还是老样子, <code>git clone</code>：</p><ul><li>git clone 会先下载下来不是lfs的普通文件，然后再下载lfs。</li><li>如果lfs下载卡住，ctrl-c后，会发现普通文件是下载好的，那么进入仓库，<code>git lfs pull</code>去拉lfs文件（<code>git pull</code>是去拉普通文件）</li></ul><h2 id="_3-添加" tabindex="-1"><a class="header-anchor" href="#_3-添加" aria-hidden="true">#</a> 3. 添加</h2><h3 id="_3-1-未add" tabindex="-1"><a class="header-anchor" href="#_3-1-未add" aria-hidden="true">#</a> 3.1. 未add</h3><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>$ <span class="token function">git</span> lfs track <span class="token string">&quot;*.tar&quot;</span>
$ <span class="token function">git</span> lfs track <span class="token string">&quot;a.tar&quot;</span>
$ <span class="token function">git</span> lfs track <span class="token string">&quot;images/*&quot;</span>

<span class="token comment"># 老一套</span>
$ <span class="token function">git</span> <span class="token function">add</span> <span class="token builtin class-name">.</span>
$ <span class="token function">git</span> commit <span class="token parameter variable">-m</span> <span class="token string">&#39;track&#39;</span>
$ <span class="token function">git</span> push
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>git lfs track &lt;file&gt;</code>会去将符合规则的东西，添加到<code>.gitattributes</code>里面。所以，直接编辑<code>.gitattributes</code>一个意思。</p><p><code>git lfs untrack &lt;file&gt;</code>移除<code>.gitattributes</code>里面相应的规则。</p><h3 id="_3-2-已经add" tabindex="-1"><a class="header-anchor" href="#_3-2-已经add" aria-hidden="true">#</a> 3.2. 已经add</h3><p>已经add的东西，用<code>git lfs track &lt;file&gt;</code>还是普通文件。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>$ <span class="token function">git</span> lfs migrate <span class="token function">import</span> --include-ref<span class="token operator">=</span>main <span class="token parameter variable">--include</span><span class="token operator">=</span><span class="token string">&quot;*.tar&quot;</span>

$ <span class="token function">git</span> <span class="token function">add</span> <span class="token builtin class-name">.</span>
$ <span class="token function">git</span> commit <span class="token parameter variable">-m</span> <span class="token string">&#39;track&#39;</span>
On branch main
Your branch and <span class="token string">&#39;origin/main&#39;</span> have diverged,
and have <span class="token number">14</span> and <span class="token number">14</span> different commits each, respectively.
  <span class="token punctuation">(</span>use <span class="token string">&quot;git pull&quot;</span> to merge the remote branch into yours<span class="token punctuation">)</span>

<span class="token comment"># 因为修改了历史，所以强制更新</span>
$ <span class="token function">git</span> push <span class="token parameter variable">--force</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="_4-huggingface" tabindex="-1"><a class="header-anchor" href="#_4-huggingface" aria-hidden="true">#</a> 4. huggingface</h2><h3 id="_4-1-gitattributes的坑" tabindex="-1"><a class="header-anchor" href="#_4-1-gitattributes的坑" aria-hidden="true">#</a> 4.1. gitattributes的坑</h3><p>hugggingface会默认提供给我们一个<code>.gitattributes</code>规则。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token comment"># 默认</span>
*.tar.* <span class="token assign-left variable">filter</span><span class="token operator">=</span>lfs <span class="token assign-left variable">diff</span><span class="token operator">=</span>lfs <span class="token assign-left variable">merge</span><span class="token operator">=</span>lfs <span class="token parameter variable">-text</span>

*.tar <span class="token assign-left variable">filter</span><span class="token operator">=</span>lfs <span class="token assign-left variable">diff</span><span class="token operator">=</span>lfs <span class="token assign-left variable">merge</span><span class="token operator">=</span>lfs <span class="token parameter variable">-text</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>这两个不是一个规则！，所以<code>xxx.tar</code>不会被查找到。</p>`,25),l=[t];function c(d,o){return s(),n("div",null,l)}const p=a(i,[["render",c],["__file","lfs.html.vue"]]);export{p as default};
